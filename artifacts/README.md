# Generated artifacts

This directory is reserved for generated local artifacts. Phase 1 permits
only the fail-closed `preflight.py` script to write `preflight-pass.json`, and
only after all gates pass. It contains no deployed addresses, transaction
hashes, or fabricated evidence.
