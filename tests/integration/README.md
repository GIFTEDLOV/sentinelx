# Integration tests

Phase 1 does not deploy or run against Studio-dev. This directory is reserved for a later integration suite that will use the pinned `studio-dev` network, fee estimation, `FeesDistribution`, `feeValue`, and finalized lifecycle reconciliation.

