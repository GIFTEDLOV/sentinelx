# Frontend placeholder

The SentinelX application UI is intentionally not implemented in Phase 1.
The frontend directory is intentionally a Phase 1 placeholder. No application
UI, wallet flow, or network writer is implemented yet.

Phase 2 must use GenLayerJS `2.0.0-rc.1` against Studio-dev (`61997`) and the
same fee-aware, single-broadcast, same-hash reconciliation standard documented
in `docs/TOOLCHAIN.md`.
