# SentinelX Studionet stable qualification family.
genlayer-py==0.18.0
# genlayer-test 0.29.2 is installed with --no-deps in the stable
# qualification environment because its published metadata still declares
# the older SDK range; the mandated stable runtime is genlayer-py 0.18.0.
genlayer-test==0.29.2
genvm-linter==0.11.0
web3==8.0.0
pytest==8.4.1
pyright==1.1.410
