# Integration workflow placeholder

The future integration workflow will exercise the deployed contracts against Studio-dev only. It must follow `PRECONDITION READ → ESTIMATE FEES → BROADCAST EXACTLY ONCE → PERSIST TX HASH → RECONCILE SAME HASH → CHECK FINALIZED LIFECYCLE → VERIFY EXECUTION RESULT → READ EXPECTED STATE`.

